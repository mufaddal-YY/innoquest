export const metadata = {
  title: 'Innoquest Consulting | Admin',
  description: '',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
